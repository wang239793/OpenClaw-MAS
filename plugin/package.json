{
  "name": "ecc-hooks",
  "version": "1.0.0",
  "description": "ECC tool execution hooks for OpenClaw",
  "type": "module",
  "main": "dist/index.mjs",
  "openclaw": {
    "extensions": [
      "./dist/index.mjs"
    ]
  },
  "scripts": {
    "build": "tsc --skipLibCheck",
    "dev": "tsc --watch --skipLibCheck"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "openclaw": "*"
  }
}
